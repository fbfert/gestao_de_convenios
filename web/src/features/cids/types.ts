export type Cid = {
  id: number
  codigo: string
  descricao: string
  ativo: boolean
}

export type CidForm = {
  codigo: string
  descricao: string
  ativo: boolean
}

export type CidPayload = {
  codigo: string
  descricao: string
  ativo: boolean
}
