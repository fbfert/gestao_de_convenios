import { useQuery, useQueryClient } from '@tanstack/react-query'
import { useState, type FormEvent } from 'react'
import { Link, useParams } from 'react-router-dom'
import { apiClient } from '../../api/client'
import { Select } from '../../components/ui/Select'
import { useEspecialidades, useProfissionais } from '../../lib/queries/useReferenceData'

type Convenio={id:number;nome:string;descricao:string|null;connector_type:string;ativo:boolean}; type Regra={id:number;tipo_terapia:string;frequencia_lancamento:string;qtd_autorizada_por_ciclo:number;validade_senha_dias:number|null;vigente_desde:string;vigente_ate:string|null}; type Valor={id:number;especialidade_id:number|null;profissional_id:number|null;valor:string;vigente_desde:string;vigente_ate:string|null}
const field='w-full rounded-xl border border-white/10 bg-slate-950 px-3 py-2 text-white'; const card='rounded-2xl border border-white/10 bg-white/5 p-5'
export function ConveniosPage(){const q=useQuery({queryKey:['convenios'],queryFn:async()=> (await apiClient.get<{data:Convenio[]}>('/convenios')).data.data});const qc=useQueryClient();const [form,setForm]=useState<Convenio|null>(null);const [nome,setNome]=useState('');const [descricao,setDescricao]=useState('');const [tipo,setTipo]=useState('manual');const [ativo,setAtivo]=useState(true);const salvar=(e:FormEvent)=>{e.preventDefault();const p={nome,descricao,connector_type:tipo,ativo};const req=form?.id?apiClient.patch(`/convenios/${form.id}`,p):apiClient.post('/convenios',p);req.then(()=>{qc.invalidateQueries({queryKey:['convenios']});setForm(null);setNome('')})};return <div className="space-y-5"><div className="flex justify-between"><div><p className="text-xs uppercase tracking-[.3em] text-cyan-300">Convênios</p><h2 className="mt-2 text-3xl font-semibold">Gestão de convênios</h2><p className="mt-2 text-sm text-slate-300">Abra um convênio para configurar regras de autorização e valores de pagamento.</p></div><button onClick={()=>{setForm({id:0,nome:'',descricao:'',connector_type:'manual',ativo:true});setNome('');setDescricao('');setTipo('manual');setAtivo(true)}} className="rounded-xl bg-cyan-400 px-4 py-2 font-semibold text-slate-950">Novo convênio</button></div>{form&&<form onSubmit={salvar} className={card}><h3 className="font-semibold">{form.id?'Editar convênio':'Novo convênio'}</h3><div className="mt-3 grid gap-3 md:grid-cols-3"><input required value={nome} onChange={e=>setNome(e.target.value)} placeholder="Nome" className={field}/><textarea value={descricao} onChange={e=>setDescricao(e.target.value)} placeholder="Descrição" className={field}/><Select value={tipo} onChange={e=>setTipo(e.target.value)}><option value="manual">Manual</option><option value="api">API</option><option value="scraping">Scraping</option></Select><label className="flex items-center gap-2"><input type="checkbox" checked={ativo} onChange={e=>setAtivo(e.target.checked)}/> Ativo</label><button className="rounded-xl bg-cyan-400 p-2 font-semibold text-slate-950">Salvar</button><button type="button" onClick={()=>setForm(null)}>Cancelar</button></div></form>}{q.data?.map(c=><div key={c.id} className={`${card} flex items-center justify-between`}><Link to={`/convenios/${c.id}`}>{c.nome}{c.descricao?<p className="text-xs text-slate-400">{c.descricao}</p>:null}</Link><div className="flex gap-3"><span>{c.ativo?'Ativo':'Inativo'}</span><button onClick={()=>{setForm(c);setNome(c.nome);setDescricao(c.descricao??'');setTipo(c.connector_type);setAtivo(c.ativo)}} className="text-cyan-200">Editar</button></div></div>)}</div>}
export function ConvenioDetalhePage(){const {id}=useParams();const qc=useQueryClient();const [tipo,setTipo]=useState('especializada');const [freq,setFreq]=useState('semanal');const [especialidade,setEspecialidade]=useState('');const [profissional,setProfissional]=useState('');const k=['convenio',id];const regras=useQuery({queryKey:[...k,'regras'],queryFn:async()=> (await apiClient.get<{data:Regra[]}>(`/convenios/${id}/regras`)).data.data});const valores=useQuery({queryKey:[...k,'valores'],queryFn:async()=> (await apiClient.get<{data:Valor[]}>(`/convenios/${id}/valores`)).data.data});const esps=useEspecialidades();const profs=useProfissionais({especialidade_id:especialidade});const post=(u:string,p:object,key:unknown[])=>apiClient.post(u,p).then(()=>qc.invalidateQueries({queryKey:key}));const enc=(u:string,key:unknown[])=>{if(confirm('Encerrar esta vigência? Isso afeta cálculos futuros.'))apiClient.patch(u).then(()=>qc.invalidateQueries({queryKey:key}))};const regra=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);post(`/convenios/${id}/regras`,{tipo_terapia:tipo,frequencia_lancamento:freq,qtd_autorizada_por_ciclo:Number(f.get('qtd')),validade_senha_dias:f.get('senha')?Number(f.get('senha')):null,vigente_desde:f.get('desde')},[...k,'regras']);e.currentTarget.reset()};const valor=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);post(`/convenios/${id}/valores`,{especialidade_id:especialidade?Number(especialidade):null,profissional_id:profissional?Number(profissional):null,valor:f.get('valor'),vigente_desde:f.get('desde')},[...k,'valores']);e.currentTarget.reset()};return <div className="space-y-6"><div className="flex items-center gap-3"><Link to="/convenios" className="text-cyan-200">← Convênios</Link><Link to="/convenios/ajuda" className="rounded-xl border border-cyan-400/40 px-3 py-1.5 text-sm font-semibold text-cyan-100 transition hover:bg-cyan-400/10">Ajuda</Link></div><h2 className="text-3xl font-semibold">Configuração do convênio</h2><section className={card}><h3 className="text-lg font-semibold">Nova regra</h3><form onSubmit={regra} className="mt-3 grid gap-3 md:grid-cols-3"><Select value={tipo} onChange={e=>setTipo(e.target.value)}><option value="especializada">Especializada</option><option value="convencional">Convencional</option><option value="outro">Outro</option></Select><Select value={freq} onChange={e=>setFreq(e.target.value)}><option value="diaria">Diária</option><option value="semanal">Semanal</option><option value="mensal">Mensal</option></Select><input name="qtd" required type="number" min="1" placeholder="Quantidade" className={field}/><input name="senha" type="number" min="1" placeholder="Validade senha (dias)" className={field}/><input name="desde" required type="date" className={field}/><button className="rounded-xl bg-cyan-400 p-2 font-semibold text-slate-950">Salvar regra</button></form><Historico items={regras.data??[]} render={r=>`${r.tipo_terapia} · ${r.frequencia_lancamento} · ${r.qtd_autorizada_por_ciclo}`} encerrar={r=>enc(`/convenios/${id}/regras/${r.id}/encerrar`,[...k,'regras'])}/></section><section className={card}><h3 className="text-lg font-semibold">Novo valor</h3><form onSubmit={valor} className="mt-3 grid gap-3 md:grid-cols-3"><Select value={especialidade} onChange={e=>{setEspecialidade(e.target.value);setProfissional('')}}><option value="">Valor geral</option>{(esps.data??[]).map(x=><option key={x.id} value={x.id}>{x.nome}</option>)}</Select><Select value={profissional} onChange={e=>setProfissional(e.target.value)} disabled={!especialidade}><option value="">Todos profissionais</option>{(profs.data??[]).map(x=><option key={x.id} value={x.id}>{x.nome}</option>)}</Select><input name="valor" required type="number" step=".01" min="0" placeholder="Valor" className={field}/><input name="desde" required type="date" className={field}/><button className="rounded-xl bg-cyan-400 p-2 font-semibold text-slate-950">Salvar valor</button></form><Historico items={valores.data??[]} render={v=>`R$ ${v.valor}`} encerrar={v=>enc(`/convenios/${id}/valores/${v.id}/encerrar`,[...k,'valores'])}/></section></div>}
function Historico<T extends {id:number;vigente_desde:string;vigente_ate:string|null}>({items,render,encerrar}:{items:T[];render:(item:T)=>string;encerrar:(item:T)=>void}){return <div className="mt-4 space-y-2">{items.map(i=><div key={i.id} className={i.vigente_ate?'text-slate-500 line-through':'text-white'}>{render(i)} · {i.vigente_desde}{i.vigente_ate?` até ${i.vigente_ate}`:<button type="button" onClick={()=>encerrar(i)} className="ml-3 text-rose-200">Encerrar</button>}</div>)}</div>}
