export type Especialidade = {
  id: number
  nome: string
  ativo: boolean
}

export type EspecialidadeForm = {
  nome: string
  ativo: boolean
}

export type EspecialidadePayload = {
  nome: string
  ativo: boolean
}
